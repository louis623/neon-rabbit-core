# Sparkle Finder Open Items

- Sparkle Suite must complete the implementation-ready prerequisites in `docs/handoffs/2026-08-25-suite-finder-compatibility-prerequisites.md` before Finder Releases 1-3 of the Dance Floor compatibility plan can proceed. The required Suite-first contracts are additive cursor/exact catalog reads, atomic net-quantity availability with lead/dancer totals, and a versioned stage-idempotent exact-variant Studio continuation. Preserve Suite v1 response fields during migration and do not mutate production data for verification.
- After those Suite contracts are deployed, capture the Suite commit, migration (if any), deployment ID, aliases, and read-only contract evidence, then implement Finder Releases 1-4 from `docs/plans/2026-08-25-suite-dance-floor-compatibility-plan.md` in order. Do not approximate pagination or pending-adjusted quantity in Finder, and do not accept an ambiguous Studio variant without exact confirmation.
- Showcase quality hardening is complete through commit `885e26d` and deployment `dpl_5i3EPnnTDM2Yg1jFNZzqJ8oZLB34` at `https://yoursparklefinder.com`. The five releases, three additive hardening migrations, full automated suite, production build, release smoke, responsive review, security setting, and canonical-domain verification all passed.
- With Louis's designated demo account and demo data, run one signed-in production visual/mutation pass covering Google OAuth return, Hero Piece save, Showcase publish/unpublish, story/photo save, Showcase Collection assignment, follow/comment/block behavior, and share controls. Do not run these mutations against an arbitrary customer account.
- A positive live public-Showcase metadata/share preview cannot be exercised until a designated public demo Showcase exists. The production collection table had no rows during the August 22 read-only launch audit; do not fabricate customer data to force this check.
- Future scale cleanup: review the 44 Supabase RLS init-plan warnings and one overlapping comment-update policy warning. Eight authenticated `SECURITY DEFINER` advisor warnings are currently intentional owner-scoped or bounded app RPCs; re-audit grants/search paths when those functions change.
- Automatic Reps endpoint completed: Suite commit `7d373f44`, migration `20260821191500_ss_finder_rep_directory_visibility.sql`, and production deployment `dpl_AwjMxR9w5C3DmMvtMeuZBWn94Fym` now return Heather/BlingKitchen from `GET /api/public/finder/reps`; Finder's live contract checker passes with `REPS=1` and demos remain hidden.
- Remaining release follow-up: run one read-only signed-in production visual check on `https://yoursparklefinder.com/reps` when an appropriate Finder demo/reviewer session is available. Run favorite add/remove persistence only with an explicitly designated smoke account because it mutates that account's saved reps. The standard local release smoke now validates the live Heather feed and rejects fixture fallback.

- Future Codex sessions should be opened from `C:\Users\louis\sparkle-finder-repo` with Workspace write enabled.
- Keep new durable project memory in repo-local `vault/`.
- Keep new implementation plans, handoffs, decisions, research, and deployment notes in repo-local `docs/`.
- Add repo-local `.agents/skills` entries when Sparkle Finder-specific skills are created.
- Sparkle Finder Vercel now has `OPENAI_API_KEY` for production and preview plus explicit Nic-Nac model env vars. Deployed linked-rep Nic-Nac stream smoke passed with a real temporary authenticated Finder account.
- Secret Rep ID claiming is deployed and smoked: Finder migration applied, shared claim token configured in Suite/Finder Vercel production/preview, service-role table grants fixed, deployed browser claim smoke passed, and temporary smoke data was cleaned up.
- Latest collector profile stats deployment: `dpl_GX6Dzj8DAM61ERf59JHbFTRwUKcf`, aliased at `https://sparkle-finder-dev.vercel.app`, with migration `20260702235634_collection_acquisition_source.sql` applied and remotely verified. Homepage stats are now `Owned`, `Wishlist`, `Diamonds`, `Unicorns`, and `Found by Sparkle Finder`; the Finder-assisted count is sourced from owned items with `sparkle_finder_lead` or `nic_nac_request`.
- Supabase migration history cleanup is complete: remote history matches local migration versions, the short `20260613` migration was normalized to `20260613000000`, `supabase/.temp` is ignored, and `supabase db push --yes` now reports `Remote database is up to date.`
- Latest signed-in UI alignment deployment: `dpl_gdKzhuuCqeKJm9cVqCX6ZDefZTGT`, aliased at `https://sparkle-finder-dev.vercel.app`. July 3 A/B/C mobile app preview is now the active signed-in UI reference; verification passed with lint, full tests, build, smoke, signed-in screenshot review, and live `200 OK` checks for `/`, `/reps`, `/library`, and `/auth/sign-in`.
- Latest public account-gate deployment: `dpl_94RcwTnZgadnTdPh5TsWXtSNBJ3b`, aliased at `https://sparkle-finder-dev.vercel.app`. Logged-out `/` now requires a Free or Silver account before customers use/see product surfaces, with `Create free account` and `Sign in` as the only primary actions; signed-in app behavior remains preserved.
- Latest sign-out affordance deployment: `dpl_7HEfccnpkPWKN56vfuaQ4ocpez1w`, aliased at `https://sparkle-finder-dev.vercel.app`. Signed-in desktop nav and the authenticated Account/Me page now expose `Sign out`; sign-out links have prefetch disabled to avoid accidental GET sign-out during Next.js link prefetch.
- Latest Bling Vault app-layout deployment: `dpl_3Dox4Dp6qvNx173ddcYoGGQi1nmy`, aliased at `https://sparkle-finder-dev.vercel.app`. The signed-in Bling Vault section now shares the A/B/C mobile app canvas, stacks Hero Piece/Wishlist/mosaic content, and no longer expands into the old wide dashboard layout.
- Continue shared Nic-Nac integration after the model adapter, linked-rep surface prompt, automatic safe Finder-memory preload, linked Suite memory bridge, product-context tool policy, mission guardrails, deployed Secret Rep ID claim UI/storage, configured OpenAI Vercel env, deployed linked-rep smoke, durable Finder Nic-Nac telemetry migration, deployed telemetry build, secured deployed telemetry runtime smoke, baseline AI/memory disclosure copy, availability/live-show read tool parity, collection/Showcase/Studio/profile read-status parity, and collection/Showcase/profile owner save tools: Finder still needs full Studio file-intake workflow exposure through app-owned uploaded file state, attorney/final policy review, broader marketing/onboarding positioning, and eventually an authenticated deployed Nic-Nac save smoke when local smoke credentials are available.
- Latest collection/Showcase/profile owner save-tools deployment: `dpl_2ykVW81bq6FhEzoNAyad8nQDDhHP`, aliased at `https://sparkle-finder-dev.vercel.app`, with Vercel `READY`, public `/`, `/library`, and `/auth/sign-in` returning `200`, and the secured telemetry smoke route returning `401` without a bearer token.
- Latest lint-health cleanup deployment: `dpl_2ysBkLzjEXBUSzas94nmbMpA3i5s`, aliased at `https://sparkle-finder-dev.vercel.app`, with `npm run lint`, strict `npx eslint . --max-warnings=0`, full tests, local production build, and Vercel production build passing.
- Latest collection/Showcase/Studio/profile read-status parity deployment: `dpl_9rdCEsULSz7DUEFFw59aJBbKARfM`, aliased at `https://sparkle-finder-dev.vercel.app`, with Vercel `READY`, public `/`, `/library`, and `/auth/sign-in` returning `200`, and the secured telemetry smoke route returning `401` without a bearer token.
- Deployed Finder Nic-Nac telemetry smoke completed June 22:
  - Migration applied and verified: `supabase/migrations/20260622173000_finder_nic_nac_conversation_telemetry.sql`.
  - Deployed build: `dpl_8tjDSHhUZ2cfvrJtQM61yAXAtNZa` aliased at `https://sparkle-finder-dev.vercel.app`.
  - Runtime smoke command: `npm run smoke:finder-telemetry-runtime`.
  - Result: row counts `{"conversations":2,"messages":4,"runs":2}`, all checks true, cleanup `ok:true`, and residual counts `0`.
  - Security check: `/api/internal/finder/nic-nac-telemetry-smoke` returns `401` without the bearer token.
