# Sparkle Finder Open Items

- Future Codex sessions should be opened from `C:\Users\louis\sparkle-finder-repo` with Workspace write enabled.
- Keep new durable project memory in repo-local `vault/`.
- Keep new implementation plans, handoffs, decisions, research, and deployment notes in repo-local `docs/`.
- Add repo-local `.agents/skills` entries when Sparkle Finder-specific skills are created.
- Configure Sparkle Finder Vercel `OPENAI_API_KEY` before calling deployed Silver Finder Nic-Nac model streaming runtime-ready. Optional model override vars now documented in `.env.example`: `NIC_NAC_HUMAN_DEFAULT_MODEL`, `NIC_NAC_HUMAN_ESCALATED_MODEL`, `NIC_NAC_UTILITY_MODEL`, and `NIC_NAC_LAB_SYNTHESIS_MODEL`.
- Continue shared Nic-Nac integration after the model adapter, linked-rep surface prompt, and automatic safe Finder-memory preload: Finder still needs the shared linked-human memory adapter across Suite/Finder, deeper product-context tool policy beyond prompt boundaries, Secret Rep ID claim UI/storage, and a real deployed authenticated Finder Nic-Nac smoke after the OpenAI key is configured.
